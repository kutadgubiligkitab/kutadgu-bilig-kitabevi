{
  "name": "kutadgu-bilig-kitabevi",
  "private": true,
  "description": "Static Vercel storefront. Browser regression lives in tests/e2e (Playwright).",
  "scripts": {
    "test:unit": "node scripts/stage91-admin-import-scale-tests.js && node scripts/catalog-visibility-tests.js && node scripts/p0-cart-favorites-identity-tests.js",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1"
  }
}
