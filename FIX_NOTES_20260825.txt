2026-08-25 Safe cleanup

1) Contact address fixed:
   Added "KAPI NO: K" to visible address and Google Maps query.

2) Font performance improved safely:
   Added full-glyph UKIJCJK.woff2 (about 1.2 MB) and made it the first @font-face source.
   Original UKIJCJK.ttf is kept as fallback, so no glyphs/files were removed.

3) CSS safety note:
   Trust-strip/carousel rules were inspected. They are layered across base/mobile/premium files and some are intentional overrides, not simple duplicates. They were NOT aggressively deleted because that could break the current desktop/mobile layouts.
