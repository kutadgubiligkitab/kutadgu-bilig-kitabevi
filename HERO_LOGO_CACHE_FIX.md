# Hero Logo Cache Fix
- Hero bookstore scene's old book-cover image was replaced with `hero-brand-logo.png`.
- A brand-new filename plus `?v=1` is used to bypass browser/Vercel caching.
- All previous WIDE-PRO + category-layout + logo + Supabase/Admin/cart/favorites/search features remain unchanged.
